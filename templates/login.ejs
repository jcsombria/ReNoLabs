<!DOCTYPE html>
<html>
	<head>
		<%- include('ui/header'); %>
		<title><%= lab_title %></title>
	</head>
	<body>
		<div class="text-center align-middle login-block">
			<div class="logo"><h1>Laboratorio Online UCM - DACYA</h1></div>
			<form style="max-width:400px; margin:auto" id="login" action="/" method="post">
				<h1 class="h4 mb-3"><%= lab_title %></h1>
				<label for="email" class="sr-only">Usuario:</label>
				<input type="text" name="username" placeholder="Usuario" id="username" class="form-control mb-3" required autofocus>
				<label for="password" class="sr-only">Contraseña:</label>
				<input type="password" name="password" placeholder="Contraseña" id="password" class="form-control mb-3">
				<button type="submit" class="btn btn-lg btn-primary btn-block" value="Submit">Entrar</button> 
				<input type="hidden" name="_csrf" value="<%= csrfToken %>">
			</form>
			<% if(message.length > 0) { %>
			<div class="alert alert-warning mt-3" role="alert"><%= message %></div>
			<% } %>
	  </div>
	</body>
</html>